# PDFs dos papers

Coloque novos PDFs nesta pasta.

O `rag_papers.py` procura primeiro em `papers/pdfs/` e depois na raiz de `papers` por compatibilidade com os arquivos antigos que ainda nao puderam ser movidos por causa de permissoes do OneDrive.